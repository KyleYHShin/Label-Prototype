<UserControl x:Class="BasicModule.Views.Rule.EditOrDeleteButtonView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d">

    <Grid>
        <StackPanel Style="{StaticResource RuleCommonButtonStackPanel}">
            <Button
                Content="편 집"
                Command="{Binding Edit}"
                Style="{StaticResource ButtonCommon}"
                Template="{StaticResource ButtonCommonTemplate}"/>

            <Button
                Content="삭 제"
                Command="{Binding Delete}"
                Style="{StaticResource ButtonCommon}"
                Template="{StaticResource ButtonCommonTemplate}"/>
        </StackPanel>
    </Grid>
</UserControl>
