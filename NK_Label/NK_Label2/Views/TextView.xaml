<UserControl x:Class="NK_Label2.Views.TextView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:NK_Label2.Views"
             xmlns:model="clr-namespace:NK_Label2.Models"
             mc:Ignorable="d" >
    <UserControl.Resources>
        <model:TextModel x:Key="TextModel"/>
    </UserControl.Resources>
    <UserControl DataContext="{StaticResource TextModel}"
                 Width="{Binding Width, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                 Height="{Binding Height, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                 Content="{Binding Text, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                 FontSize="{Binding FontSize, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                 FontFamily="{Binding FontFamily, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}">
    </UserControl>
</UserControl>
