<Window x:Class="NK_Label.Views.SubWindowView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:NK_Label.Views"
        xmlns:system="clr-namespace:NK_Label.Settings"
        xmlns:viewmodel="clr-namespace:NK_Label.ViewModels"
        mc:Ignorable="d"
        Title="NK-Label"
        Height="300" Width="400">

    <Window.Resources>
        <viewmodel:LabelViewModel x:Key="LabelData"/>
    </Window.Resources>

    <Window.DataContext>
        <Binding Source="{StaticResource LabelData}"/>
    </Window.DataContext>
    
    <Border>
        <AdornerDecorator>
            <DockPanel x:Name="DockPanelWindow"
                       Background="{Binding Path=WindowBackground, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                       Margin="0">
                
            </DockPanel>
        </AdornerDecorator>
    </Border>
</Window>
